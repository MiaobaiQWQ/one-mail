#!/bin/sh

if [ "${OLLAMA_COMMIT_DISABLE:-}" = "1" ]; then
  exit 0
fi

repo_root=$(git rev-parse --show-toplevel 2>/dev/null) || exit 0
script="$repo_root/scripts/ollama-commit-message.mjs"

if [ ! -f "$script" ]; then
  exit 0
fi

node "$script" "$@"
